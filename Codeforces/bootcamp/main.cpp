#include <bits/stdc++.h>

#define int long long
#define INF 100000007
#define all(m) m.begin(), m.end()
#define readV(m) for(auto &it : m) cin >> it;

typedef std::vector<int> vi;
typedef std::vector<std::vector<int>> vvi;

using namespace std;

int32_t main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);


    return 0;
}